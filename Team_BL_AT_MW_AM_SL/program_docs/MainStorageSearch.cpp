#include "MainStorageSearch.h"

/*
@pre
@post
@param title
@param headNode
@return node vector of matching nodes
*/
std::vector<MainStorageNode*>* MainStorageSearch::titleFind (std::string title, MainStorageNode* headNode)
{
	// search title BST for nodes matching title
	// push each matching Node pointer into resultNodes
	// continue in BST until all matches found
	std::vector<MainStorageNode*>* resultNodes = new std::vector<MainStorageNode*> ();
	return resultNodes;
}

std::vector<MainStorageNode*>* MainStorageSearch::yearFind (int year, MainStorageNode* headNode)
{
	std::vector<MainStorageNode*>* resultNodes = new std::vector<MainStorageNode*> ();
	return resultNodes;
}
std::vector<MainStorageNode*>* MainStorageSearch::contentRatingFind (std::string contentRating, MainStorageNode* headNode)
{
	std::vector<MainStorageNode*>* resultNodes = new std::vector<MainStorageNode*> ();
	return resultNodes;
}
std::vector<MainStorageNode*>* MainStorageSearch::ratingFind (double rating, MainStorageNode* headNode)
{
	std::vector<MainStorageNode*>* resultNodes = new std::vector<MainStorageNode*> ();
	return resultNodes;
}
std::vector<MainStorageNode*>* MainStorageSearch::genreFind (std::string genre, MainStorageNode* headNode)
{
	std::vector<MainStorageNode*>* resultNodes = new std::vector<MainStorageNode*> ();
	return resultNodes;
}
std::vector<MainStorageNode*>* MainStorageSearch::intersection (std::vector<MainStorageNode*>* nodeVector1, std::vector<MainStorageNode*>* nodeVector2)
{
	std::vector<MainStorageNode*>* resultNodes = new std::vector<MainStorageNode*> ();
	return resultNodes;
}